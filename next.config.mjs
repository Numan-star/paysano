/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      unoptimized: true,
    },
    // output: 'export', // Uncomment this line if you intend to use static export
  };
  
  export default nextConfig;
  